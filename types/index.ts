export interface Product {
    id: number;
    name: string;
    category: string;
    price: number;
    image: string;
}

export interface HeroItem {
    id: string;
    subtitle: string;
    title: string;
    desc: string;
    image: string;
    specs: string[];
    color: string;
}
